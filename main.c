#include "proto_parti.h"

int main()
{
    //put_ID(0);
   // put_Nombre_Enr(0);

   //   /*
   struct candidats candi;

   cand_info(&candi);

   printf("%s", candi.Nom);
   printf("%s", candi.Prenom);
     /*
    int yes=0;
    do{
    menu_PP();

    }while(1);

    return 0;
    /* */
}

